(function () {
  const root = document.getElementById('root');
  const KEYS = { planning:'vlc.vehiclePlanningData', planningRaw:'vlc.vehiclePlanningRawData', status:'vlc.vehicleStatusTrackingData', statusRaw:'vlc.vehicleStatusTrackingRawData', raipur:'vlc.raipurDatabaseData', raipurRaw:'vlc.raipurDatabaseRawData', gateIn:'vlc.gateInData', gateOut:'vlc.gateOutData', gateInMeta:'vlc.gateInMeta', gateOutMeta:'vlc.gateOutMeta', headerSettings:'vlc.headerSettings', loadingMappings:'vlc.loadingPointMappings' };
  const state = { page:'dashboard', sidebar:true, workbook:null, workbookDate1904:false, file:null, selectedSheet:'', search:'', pageSize:15, pageNo:1, uploadRole:'gateIn', message:'', planningPaste:'', statusPaste:'', raipurPaste:'', planningPreview:null, statusPreview:null, raipurPreview:null, filters:{}, header:read('vlc.headerSettings',{fy:'2026-2027',period:'P06',dateRange:'17 Aug - 13 Sep'}) };
  const nav = [['dashboard','Dashboard','⌂'],['planning','Vehicle Planning','▣'],['status','Vehicle Status Records','◈'],['raipur','Raipur Database','▤'],['upload','Excel Upload','⇧']];
  const names = {dashboard:'Dashboard',planning:'Vehicle Planning',status:'Vehicle Status Records',raipur:'Raipur Database',upload:'Excel Upload'};
  const planningColumns=['S.No','Date','Loc','Plant','CFA','Weight','STO','Loading','Vehicle In','Vehicle Number','Vehicle Out','Slip Number'];
  const statusColumns=['S No','Demanded Date','Required Date','Loading Pt.','Location','Weight','Vehicle Arrived','Vehicle Dispatch','Remarks','Gate Slip No.'];
  function read(key,fallback){try{const r=localStorage.getItem(key);return r?JSON.parse(r):(fallback??[])}catch{return fallback??[]}}
  const DATA_VERSION_KEY='vlc.dataVersion';
  const CURRENT_DATA_VERSION='date-v5';
  function ensureDateDataVersion(){const current=localStorage.getItem(DATA_VERSION_KEY);if(current===CURRENT_DATA_VERSION)return;if(current===null||current!==CURRENT_DATA_VERSION){const keys=[KEYS.planning,KEYS.planningRaw,KEYS.status,KEYS.statusRaw,KEYS.raipur,KEYS.raipurRaw,KEYS.gateIn,KEYS.gateOut,KEYS.gateInMeta,KEYS.gateOutMeta];const prefix=`vlc.legacy.${current||'unknown'}.`;keys.forEach(k=>{const v=localStorage.getItem(k);if(v!==null)localStorage.setItem(prefix+k,v);localStorage.removeItem(k)});localStorage.setItem(DATA_VERSION_KEY,CURRENT_DATA_VERSION)}}
  function write(key,value){localStorage.setItem(key,JSON.stringify(value));window.dispatchEvent(new Event('vlc-data-changed'))}
  function text(v){return String(v??'').replace(/\u00a0/g,' ').trim().replace(/\s+/g,' ')}
  function norm(v){return text(v).toUpperCase()}
  function slipText(v){return v===null||v===undefined?'':String(v).replace(/\u00a0/g,' ').trim()}
  function getLoadingMappings(){return read(KEYS.loadingMappings,[]).filter(m=>m&&text(m.source)&&text(m.target))}
  function loadingPointsMatch(p1,p2,mappings=getLoadingMappings()){const a=norm(p1),b=norm(p2);if(!a||!b)return false;if(a===b)return true;return mappings.some(m=>norm(m.source)===a&&norm(m.target)===b)}
  function saveLoadingMapping(source,target){const a=text(source),b=text(target);if(!a||!b)return{ok:false,message:'Both Loading Point values are required.'};const rows=getLoadingMappings();if(rows.some(m=>norm(m.source)===norm(a)&&norm(m.target)===norm(b)))return{ok:false,message:'This Loading Point mapping already exists.'};rows.push({id:`${norm(a)}=>${norm(b)}`,source:a,target:b});write(KEYS.loadingMappings,rows);return{ok:true}}
  function updateLoadingMapping(id,source,target){const a=text(source),b=text(target);if(!a||!b)return{ok:false,message:'Both Loading Point values are required.'};const rows=getLoadingMappings();if(rows.some(m=>m.id!==id&&norm(m.source)===norm(a)&&norm(m.target)===norm(b)))return{ok:false,message:'This Loading Point mapping already exists.'};write(KEYS.loadingMappings,rows.map(m=>m.id===id?{...m,id:`${norm(a)}=>${norm(b)}`,source:a,target:b}:m));return{ok:true}}
  function deleteLoadingMapping(id){write(KEYS.loadingMappings,getLoadingMappings().filter(m=>m.id!==id))}
  function loose(v){return norm(v).replace(/[\s\-_\/.]+/g,'')}
  const months={jan:1,feb:2,mar:3,apr:4,may:5,jun:6,jul:7,aug:8,sep:9,oct:10,nov:11,dec:12};
  const monthNames=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
  function pad2(n){return String(n).padStart(2,'0')}
  function pad4(n){return String(n).padStart(4,'0')}
  function validBusinessDateParts(y,m,d){if(!Number.isInteger(y)||!Number.isInteger(m)||!Number.isInteger(d)||m<1||m>12||d<1)return false;const leap=y%4===0&&(y%100!==0||y%400===0);const md=[31,leap?29:28,31,30,31,30,31,31,30,31,30,31];return d<=md[m-1]}
  function canonicalFromParts(y,m,d){return validBusinessDateParts(y,m,d)?`${pad4(y)}-${pad2(m)}-${pad2(d)}`:''}
  function excelSerialToBusinessDate(serial,date1904){const n=Number(serial);if(!Number.isFinite(n)||n<0||n>2958465)return'';let days=Math.floor(n),year=date1904?1904:1900;if(!date1904){if(days>=60)days-=1;days-=1}while(true){const leap=year%4===0&&(year%100!==0||year%400===0),yd=leap?366:365;if(days<yd)break;days-=yd;year+=1}const leap=year%4===0&&(year%100!==0||year%400===0),md=[31,leap?29:28,31,30,31,30,31,31,30,31,30,31];let month=1;for(const count of md){if(days<count)break;days-=count;month+=1}return canonicalFromParts(year,month,days+1)}
  function parseBusinessDate(v,opts){const fallbackYear=opts&&Number.isInteger(opts.fallbackYear)?opts.fallbackYear:new Date().getFullYear();if(v instanceof Date&&!isNaN(v.getTime()))return canonicalFromParts(v.getFullYear(),v.getMonth()+1,v.getDate());if(typeof v==='number')return excelSerialToBusinessDate(v,Boolean(opts&&opts.date1904));const raw=text(v);if(!raw)return'';if(/^\d+(?:\.\d+)?$/.test(raw)&&Number(raw)>=0&&Number(raw)<=2958465)return excelSerialToBusinessDate(Number(raw),Boolean(opts&&opts.date1904));let m=raw.match(/^(\d{4})[-\/.](\d{1,2})[-\/.](\d{1,2})/);if(m)return canonicalFromParts(+m[1],+m[2],+m[3]);m=raw.match(/^(\d{1,2})[-\/.](\d{1,2})[-\/.](\d{2,4})/);if(m){const y=+m[3]<100?2000+(+m[3]):+m[3];return canonicalFromParts(y,+m[2],+m[1])}m=raw.match(/^(\d{1,2})[-\s]([A-Za-z]{3,})(?:[-\s](\d{2,4}))?/);if(m){const mo=months[m[2].slice(0,3).toLowerCase()],y=m[3]?(+m[3]<100?2000+(+m[3]):+m[3]):fallbackYear;if(mo)return canonicalFromParts(y,mo,+m[1])}return''}
  function dateKey(v){return parseBusinessDate(v)}
  function dateDisplay(v){const canonical=parseBusinessDate(v);if(!canonical)return text(v);const [y,m,d]=canonical.split('-').map(Number);return `${pad2(d)}-${monthNames[m-1]}-${y}`}
  function groupKey(date,cfa,loading){return [dateKey(date),norm(cfa),norm(loading)].join('|')}
  function splitMulti(v){return text(v).split(/[,;\n|\/]+/).map(x=>x.trim()).filter(Boolean)}
  function validMulti(v){const s=text(v);return s&&!['-','—','NA','N/A','NULL'].includes(norm(s))}
  function carry(rows,fields){const prev=Object.fromEntries(fields.map(f=>[f,'']));return rows.map(r=>{if(r&&r.__batchBoundary)fields.forEach(f=>prev[f]='');const n={...r};delete n.__batchBoundary;fields.forEach(f=>{if(text(n[f]))prev[f]=n[f];else if(prev[f])n[f]=prev[f]});return n})}
  function unique(values){const seen=new Set(),out=[];values.flatMap(v=>Array.isArray(v)?v:[v]).forEach(v=>{const s=text(v),k=norm(s);if(s&&!seen.has(k)){seen.add(k);out.push(s)}});return out}
  function weightMT(v){const raw=text(v).replace(/,/g,'');if(!raw)return 0;const m=raw.match(/(-?\d+(?:\.\d+)?)\s*(kg|kgs|kilogram|kilograms|g|gram|grams|ton|tons|tonne|tonnes|mt|metric\s*ton|metric\s*tons)?/i);if(!m)return 0;const n=Number(m[1]);if(!Number.isFinite(n))return 0;const u=(m[2]||'MT').toLowerCase().replace(/\s+/g,' ');if(u==='g'||u.startsWith('gram'))return n/1000000;if(u==='kg'||u==='kgs'||u.startsWith('kilogram'))return n/1000;return n}
  const aliases={planning:{date:['date','plan date'],loc:['loc','location'],plant:['plant'],cfa:['cfa','cfa name'],weight:['weight','wt'],loading:['loading','loading point','loading pt','loading pt.']},status:{demandedDate:['demanded date','demand date','date'],requiredDate:['required date'],location:['location','cfa'],loadingPoint:['loading pt','loading pt.','loading point','loading'],weight:['weight','wt']},raipur:{date:['date','plan date'],loc:['loc','location'],plant:['plant'],cfa:['cfa','cfa name'],weight:['weight','wt'],loading:['loading','loading point','loading pt','loading pt.'],vehicleIn:['vehicle in','vehicle in date','gate in date','in date'],vehicleNumber:['vehicle number','vehicle no','vehicle no.'],vehicleOut:['vehicle out','vehicle out date','gate out date','out date'],slipNumber:['slip number','gate slip number','gate slip no','gate slip no.']}};
  function findIdx(headers,names){if(!names)return-1;const h=headers.map(loose);for(const a of names){const t=loose(a);if(!t)continue;const i=h.indexOf(t);if(i>=0)return i}for(const a of names){const t=loose(a);if(!t)continue;const i=h.findIndex(x=>x&& (x.includes(t)||t.includes(x)));if(i>=0)return i}return-1}
  function parseHeaderlessRaipur(c){const r={date:c[0]||'',loc:c[1]||'',plant:c[2]||'',cfa:c[3]||'',weight:c[4]||'',sto:[],loading:'',vehicleIn:'',vehicleNumber:'',vehicleOut:'',slipNumber:''};const tail=c.slice(5);if(tail.length>=5){const li=tail.length-5;r.sto=tail.slice(0,li).flatMap(v=>splitMulti(v)).filter(validMulti);r.loading=tail[li]||'';r.vehicleIn=tail[li+1]||'';r.vehicleNumber=tail[li+2]||'';r.vehicleOut=tail[li+3]||'';r.slipNumber=tail[li+4]||'';}else{r.sto=tail.slice(0,Math.max(0,tail.length-1)).flatMap(v=>splitMulti(v)).filter(validMulti);r.loading=tail.length?tail[tail.length-1]:'';}return r}
  function parsePaste(raw,kind){const lines=String(raw||'').split(/\r?\n/).map(x=>x.replace(/\r$/,'')).filter(x=>x.trim());if(!lines.length)return{headers:[],rows:[]};const delim=lines[0].includes('\t')?'\t':lines[0].includes('|')?'|':',';const split=l=>l.split(delim).map(s=>s.trim().replace(/^"|"$/g,''));const first=split(lines[0]);const a=aliases[kind];const headerMatchCount=Object.values(a).filter(n=>findIdx(first,n)>=0).length;const hasHeader=headerMatchCount>=(kind==='planning'?4:kind==='raipur'?6:3);const headers=hasHeader?first:[];const data=hasHeader?lines.slice(1):lines;
    if(kind==='planning'||kind==='raipur'){const idx={date:findIdx(headers,a.date),loc:findIdx(headers,a.loc),plant:findIdx(headers,a.plant),cfa:findIdx(headers,a.cfa),weight:findIdx(headers,a.weight),loading:findIdx(headers,a.loading),vehicleIn:findIdx(headers,a.vehicleIn||[]),vehicleNumber:findIdx(headers,a.vehicleNumber||[]),vehicleOut:findIdx(headers,a.vehicleOut||[]),slipNumber:findIdx(headers,a.slipNumber||[])};const stoIdx=headers.map((h,i)=>loose(h)==='STO'||loose(h).startsWith('STO')||loose(h).includes('STONUMBER')?i:-1).filter(i=>i>=0);return{headers,rows:data.map(line=>{const c=split(line);if(!hasHeader&&kind==='planning')return{date:c[0]||'',loc:c[1]||'',plant:c[2]||'',cfa:c[3]||'',weight:c[4]||'',sto:c.slice(5,Math.max(5,c.length-1)).flatMap(splitMulti),loading:c[c.length-1]||''};if(!hasHeader&&kind==='raipur')return parseHeaderlessRaipur(c);return{date:idx.date>=0?c[idx.date]||'':'',loc:idx.loc>=0?c[idx.loc]||'':'',plant:idx.plant>=0?c[idx.plant]||'':'',cfa:idx.cfa>=0?c[idx.cfa]||'':'',weight:idx.weight>=0?c[idx.weight]||'':'',loading:idx.loading>=0?c[idx.loading]||'':'',sto:stoIdx.flatMap(i=>splitMulti(c[i]||'')),vehicleIn:idx.vehicleIn>=0?c[idx.vehicleIn]||'':'',vehicleNumber:idx.vehicleNumber>=0?c[idx.vehicleNumber]||'':'',vehicleOut:idx.vehicleOut>=0?c[idx.vehicleOut]||'':'',slipNumber:idx.slipNumber>=0?c[idx.slipNumber]||'':''}}).filter(r=>Object.entries(r).some(([k,v])=>k==='sto'?v.length:v))};}
    const idx=Object.fromEntries(Object.entries(a).map(([k,n])=>[k,findIdx(headers,n)]));const keys=Object.keys(a);return{headers,rows:data.map(line=>{const c=split(line);if(!hasHeader&&kind==='status')return{demandedDate:c[0]||'',requiredDate:c[1]||'',loadingPoint:c[2]||'',location:c[3]||'',weight:c[4]||''};return Object.fromEntries(keys.map((k,i)=>[k,hasHeader?(idx[k]>=0?c[idx[k]]||'':''):c[i]||'']))}).filter(r=>Object.values(r).some(Boolean))}
  }
  function aggregatePlanning(rows){const m=new Map();carry(rows,['date','plant','cfa','loading']).map(r=>({...r,sto:(Array.isArray(r.sto)?r.sto:splitMulti(r.sto)).filter(validMulti)})).forEach((r,i)=>{const k=groupKey(r.date,r.cfa,r.loading);if(k==='||')return;const loc=text(r.loc)||'—';const g=m.get(k)||{date:r.date,plant:[],cfa:r.cfa,weight:0,loading:r.loading,locations:new Map(),raw:[]};g.plant.push(r.plant);g.weight+=weightMT(r.weight);let lg=g.locations.get(norm(loc));if(!lg){lg={loc,weightMT:0,stos:[]};g.locations.set(norm(loc),lg)}lg.weightMT+=weightMT(r.weight);lg.stos.push(...(Array.isArray(r.sto)?r.sto:splitMulti(r.sto)));g.raw.push(i);m.set(k,g)});return Array.from(m.entries()).map(([k,g],i)=>{const locations=Array.from(g.locations.values()).map(l=>({...l,stos:unique(l.stos)}));return{serialNo:i+1,groupKey:k,date:g.date,loc:locations.map(l=>l.loc),plant:unique(g.plant),cfa:g.cfa,weight:`${g.weight.toFixed(3)} MT`,sto:unique(locations.flatMap(l=>l.stos)),loading:g.loading,locations,totalWeightMT:g.weight,rawRowIndexes:g.raw}})}
  function aggregateRaipur(rows){const m=new Map();carry(rows,['date','plant','cfa','loading']).map(r=>({...r,sto:(Array.isArray(r.sto)?r.sto:splitMulti(r.sto)).filter(validMulti)})).forEach((r,i)=>{const k=groupKey(r.date,r.cfa,r.loading);if(k==='||')return;const g=m.get(k)||{date:r.date,plant:[],cfa:r.cfa,weight:0,loading:r.loading,locations:new Map(),vehicleIn:[],vehicleNumber:[],vehicleOut:[],slipNumber:[],raw:[]};g.plant.push(r.plant);g.weight+=weightMT(r.weight);const loc=text(r.loc)||'—';let lg=g.locations.get(norm(loc));if(!lg){lg={loc,weightMT:0,stos:[],vehicleIn:[],vehicleNumber:[],vehicleOut:[],slipNumber:[]};g.locations.set(norm(loc),lg)}lg.weightMT+=weightMT(r.weight);lg.stos.push(...r.sto);lg.vehicleIn.push(r.vehicleIn);lg.vehicleNumber.push(r.vehicleNumber);lg.vehicleOut.push(r.vehicleOut);lg.slipNumber.push(r.slipNumber);g.vehicleIn.push(r.vehicleIn);g.vehicleNumber.push(r.vehicleNumber);g.vehicleOut.push(r.vehicleOut);g.slipNumber.push(r.slipNumber);g.raw.push(i);m.set(k,g)});return Array.from(m.entries()).map(([k,g],i)=>{const locations=Array.from(g.locations.values()).map(l=>({...l,stos:unique(l.stos),vehicleIn:unique(l.vehicleIn)[0]||'',vehicleNumber:unique(l.vehicleNumber)[0]||'',vehicleOut:unique(l.vehicleOut)[0]||'',slipNumber:unique(l.slipNumber)[0]||''}));return{serialNo:i+1,groupKey:k,date:g.date,loc:locations.map(l=>l.loc),plant:unique(g.plant),cfa:g.cfa,weight:`${g.weight.toFixed(3)} MT`,sto:unique(locations.flatMap(l=>l.stos)),loading:g.loading,locations,vehicleIn:unique(g.vehicleIn)[0]||'',vehicleNumber:unique(g.vehicleNumber)[0]||'',vehicleOut:unique(g.vehicleOut)[0]||'',slipNumber:unique(g.slipNumber)[0]||'',totalWeightMT:g.weight,rawRowIndexes:g.raw}})}
  function aggregateStatus(rows){const m=new Map();carry(rows,['demandedDate','requiredDate','location','loadingPoint']).forEach((r,i)=>{const k=groupKey(r.demandedDate,r.location,r.loadingPoint);if(k==='||')return;const g=m.get(k)||{demandedDate:r.demandedDate,requiredDate:[],location:r.location,loadingPoint:r.loadingPoint,weight:0,raw:[]};g.requiredDate.push(r.requiredDate);g.weight+=weightMT(r.weight);g.raw.push(i);m.set(k,g)});return Array.from(m.entries()).map(([k,g],i)=>({serialNo:i+1,groupKey:k,demandedDate:g.demandedDate,requiredDate:unique(g.requiredDate).join(' / '),location:g.location,loadingPoint:g.loadingPoint,weight:`${g.weight.toFixed(3)} MT`,rawRowIndexes:g.raw}))}
  function header(headers,names){const i=findIdx(headers,names);return i>=0?headers[i]:''}
  const gateAliases={sto:['STO','STO Number','STO No','STO Number(s)','Stock Transfer Order'],slip:['Gate Slip Number','Gate Slip No','Slip Number','Slip No'],inDate:['Gate In Date','Vehicle In','Vehicle In Date','In Date'],outDate:['Gate Out Date','Vehicle Out','Vehicle Out Date','Out Date'],vehicle:['Vehicle Number','Vehicle No','Registration Number'],date:['Date','Plan Date','Demanded Date'],cfa:['CFA','CFA Name','Location'],loading:['Loading','Loading Point','Loading Pt.']};
  function gateIndex(rows,role){const headers=Object.keys(rows[0]||{}),cols={sto:header(headers,gateAliases.sto),slip:header(headers,gateAliases.slip),inDate:header(headers,gateAliases.inDate),outDate:header(headers,gateAliases.outDate),vehicle:header(headers,gateAliases.vehicle),date:header(headers,gateAliases.date),cfa:header(headers,gateAliases.cfa),loading:header(headers,gateAliases.loading)},slipMap=new Map(),stoMap=new Map();rows.forEach(r=>{const slip=slipText(r[cols.slip]);if(!slip)return;const item=slipMap.get(slip)||{slipNumber:slip,vehicleIn:'',vehicleOut:'',vehicleNumber:'',date:'',cfa:'',loading:''};if(role==='gateIn'){item.vehicleIn=item.vehicleIn||(cols.inDate?r[cols.inDate]:'');item.vehicleNumber=item.vehicleNumber||(cols.vehicle?r[cols.vehicle]:'');item.date=item.date||(cols.date?r[cols.date]:'');item.cfa=item.cfa||(cols.cfa?r[cols.cfa]:'');item.loading=item.loading||(cols.loading?r[cols.loading]:'');splitMulti(r[cols.sto]).forEach(s=>{const n=norm(s),set=stoMap.get(n)||new Set();set.add(slip);stoMap.set(n,set)})}else item.vehicleOut=item.vehicleOut||(cols.outDate?r[cols.outDate]:'');slipMap.set(slip,item)});return{slipMap,stoMap}}
  function enrichPlanning(){const rows=read(KEYS.planning,[]),gi=gateIndex(read(KEYS.gateIn,[]),'gateIn'),go=gateIndex(read(KEYS.gateOut,[]),'gateOut'),groups=new Map(),conf=new Map();rows.forEach(r=>{const g=groupKey(r.date,r.cfa,r.loading),c=new Set();splitMulti(r.sto).forEach(s=>(gi.stoMap.get(norm(s))||[]).forEach(x=>c.add(x)));gi.slipMap.forEach(item=>{if(item.slipNumber&&dateKey(item.date)===dateKey(r.date)&&norm(item.cfa)===norm(r.cfa)&&norm(item.loading)===norm(r.loading))c.add(item.slipNumber)});if(c.size>1)conf.set(g,[...c]);if(c.size===1)groups.set(g,[...c][0])});return rows.map((r,i)=>{const g=groupKey(r.date,r.cfa,r.loading),slip=groups.get(g)||'',matched=splitMulti(r.sto).some(s=>(gi.stoMap.get(norm(s))||new Set()).has(slip)),ii=matched?gi.slipMap.get(slip):null,oo=matched?go.slipMap.get(slip):null;return{...r,serialNo:i+1,vehicleIn:ii?.vehicleIn||'',vehicleNumber:ii?.vehicleNumber||'',vehicleOut:oo?.vehicleOut||'',slipNumber:slip,conflict:conf.get(g)||null}})}
  function enrichStatus(){const status=read(KEYS.status,[]),planning=enrichPlanning(),gi=gateIndex(read(KEYS.gateIn,[]),'gateIn'),go=gateIndex(read(KEYS.gateOut,[]),'gateOut'),maps=getLoadingMappings(),map=new Map();planning.forEach(r=>{const g=groupKey(r.date,r.cfa,r.loading);if(!r.slipNumber)return;const set=map.get(g)||new Set();set.add(r.slipNumber);map.set(g,set)});return status.map((r,i)=>{const exact=groupKey(r.demandedDate,r.location,r.loadingPoint);let slips=map.get(exact)||new Set();if(!slips.size){const candidates=planning.filter(p=>dateKey(p.date)===dateKey(r.demandedDate)&&norm(p.cfa)===norm(r.location)&&loadingPointsMatch(p.loading,r.loadingPoint,maps));const candidateSlips=new Set(candidates.map(p=>slipText(p.slipNumber)).filter(Boolean));if(candidateSlips.size===1)slips=candidateSlips}const slip=slips.size===1?[...slips][0]:'',ii=slip?gi.slipMap.get(slip):null,oo=slip?go.slipMap.get(slip):null,arr=ii?.vehicleIn||'',out=oo?.vehicleOut||'';return{...r,serialNo:i+1,vehicleArrived:arr,vehicleDispatch:out,remarks:arr&&out?'Dispatched':arr?'Onloading':'Pending',gateSlipNo:slip,conflict:slips.size>1?[...slips]:null}})}

  function preview(kind,raw){const p=parsePaste(raw,kind),final=kind==='planning'?aggregatePlanning(p.rows):kind==='raipur'?aggregateRaipur(p.rows):aggregateStatus(p.rows);return{...p,rawRowCount:p.rows.length,finalRows:final,finalRowCount:final.length}}
  function displayPending(v){return text(v)?v:'Pending'}
  function loadingCategory(v){const n=loose(v);if(n.includes('BAKAL'))return 'B';if(n.includes('TOLAGAON'))return 'T';return ''}
  function loadingPendingCounts(){return enrichPlanning().reduce((acc,r)=>{if(text(r.vehicleIn))return acc;const c=loadingCategory(r.loading);if(c==='B')acc.B+=1;if(c==='T')acc.T+=1;return acc},{B:0,T:0})}
  function vehiclePlanningOverview(){return enrichPlanning().reduce((a,r)=>{const i=text(r.vehicleIn),o=text(r.vehicleOut);if(i&&o)a.dispatched+=1;else if(i)a.onloading+=1;else a.pending+=1;return a},{dispatched:0,onloading:0,pending:0})}
  function calculateCorePending(){return null}
  function calculatePartialPending(){return null}
  function dashboardGroupKey(date,name,loading){return [dateKey(date),norm(name),norm(loading)].join('|')}
  function dashboardAlerts(){const planning=read(KEYS.planning,[]),status=read(KEYS.status,[]),maps=getLoadingMappings(),pm=new Map(),sm=new Map();planning.forEach(r=>{const k=dashboardGroupKey(r.date,r.cfa,r.loading);if(k!=='||')pm.set(k,r)});status.forEach(r=>{const k=dashboardGroupKey(r.demandedDate,r.location,r.loadingPoint);if(k!=='||')sm.set(k,r)});const out=[],matchedP=new Set(),matchedS=new Set();pm.forEach((r,k)=>{if(sm.has(k)){matchedP.add(k);matchedS.add(k);return}const mapped=[...sm.entries()].filter(([sk,v])=>dateKey(v.demandedDate)===dateKey(r.date)&&norm(v.location)===norm(r.cfa)&&loadingPointsMatch(r.loading,v.loadingPoint,maps));if(mapped.length===1){matchedP.add(k);matchedS.add(mapped[0][0])}else{const sameDateCfa=Array.from(sm.values()).some(v=>dateKey(v.demandedDate)===dateKey(r.date)&&norm(v.location)===norm(r.cfa));out.push({date:r.date,name:r.cfa,loading:r.loading,remarks:sameDateCfa?'Loading Point Match Required':'Vehicle Call Pending'})}});sm.forEach((r,k)=>{if(matchedS.has(k))return;const sameDateCfa=Array.from(pm.values()).some(v=>dateKey(v.date)===dateKey(r.demandedDate)&&norm(v.cfa)===norm(r.location));out.push({date:r.demandedDate,name:r.location,loading:r.loadingPoint,remarks:sameDateCfa?'Loading Point Match Required':'Plan Pending'})});return out}

  function dashboardMetrics(){return{totalPlannedVehicles:read(KEYS.planning,[]).length,vehicleCalled:read(KEYS.status,[]).length,pendingLoading:loadingPendingCounts()}}
  function saveHeaderSettings(){const fy=document.getElementById('headerFy')?.value.trim()||'2026-2027';const period=document.getElementById('headerPeriod')?.value.trim()||'P06';const dateRange=document.getElementById('headerDateRange')?.value.trim()||'17 Aug - 13 Sep';state.header={fy,period,dateRange};write(KEYS.headerSettings,state.header);state.message='Header settings saved.';render()}
  function render(){root.innerHTML=`<div class="app"><header class="header"><div class="brand"><button class="menu-toggle" id="menuBtn">☰</button><div class="logo">SV</div><div><div class="app-title">STO &amp; Vehicle Management System</div><small>Supply Chain Operations</small></div></div><div class="headRight"><div class="period-block"><div class="period-summary">FY (${esc(state.header.fy)}) (${esc(state.header.period)}) ${esc(state.header.dateRange)}</div><div class="period-editor"><label>FY <input id="headerFy" value="${esc(state.header.fy)}"></label><label>Period <input id="headerPeriod" value="${esc(state.header.period)}"></label><label>Date Range <input id="headerDateRange" value="${esc(state.header.dateRange)}"></label><button class="header-save" id="saveHeader">Save</button></div></div><span>${new Intl.DateTimeFormat('en-IN',{dateStyle:'medium'}).format(new Date())}</span><div class="user"><div class="avatar">MU</div><strong>MIS User</strong></div></div></header><aside class="side ${state.sidebar?'':'collapsed'}"><div class="sectionTitle">Application</div><div class="nav">${nav.map(n=>`<button class="${state.page===n[0]?'active':''}" data-nav="${n[0]}"><span style="display:inline-block;width:24px">${n[2]}</span>${n[1]}</button>`).join('')}</div><div style="margin-top:auto;padding:12px;font-size:11px;color:#697781"><strong>System</strong><div style="margin-top:7px;color:#0b5e3c">● Ready</div></div></aside><main class="content"><div class="watermark" aria-hidden="true">Manish Pandey</div><div class="crumb"><span>Home</span><span>›</span><strong>${names[state.page]}</strong></div>${pageHtml()}</main><button id="backToTop" class="back-to-top hidden" aria-label="Back to top">↑ Top</button><footer class="footer"><span>System Status: <strong>Ready</strong></span><span>STO &amp; Vehicle Management System | v0.4.0</span></footer></div>`;bind()}
  function pageHtml(){if(state.page==='dashboard')return dashboardHtml();if(state.page==='planning')return planningHtml();if(state.page==='status')return statusHtml();if(state.page==='raipur')return raipurHtml();return uploadHtml()}
  function dashboardHtml(){const m=dashboardMetrics(),a=dashboardAlerts(),o=vehiclePlanningOverview(),total=Math.max(1,o.dispatched+o.onloading+o.pending);return `<div class="kpis"><div class="kpi"><span>Total Planned Vehicles</span><strong>${m.totalPlannedVehicles}</strong></div><div class="kpi"><span>Vehicle Called</span><strong>${m.vehicleCalled}</strong></div><div class="kpi"><span>Pending Vehicles</span><strong>—</strong></div><div class="kpi"><span>Dispatched Vehicles</span><strong>—</strong></div><div class="kpi"><span>Cancelled Vehicles</span><strong>—</strong></div></div><div class="grid2"><div class="panel mini"><h3>Vehicle Planning Overview</h3><div class="inside planning-chart"><div class="chart-row"><span>Dispatched</span><strong>${o.dispatched}</strong><div class="chart-track"><i style="width:${Math.round(o.dispatched/total*100)}%"></i></div></div><div class="chart-row"><span>Onloading</span><strong>${o.onloading}</strong><div class="chart-track"><i style="width:${Math.round(o.onloading/total*100)}%"></i></div></div><div class="chart-row"><span>Pending</span><strong>${o.pending}</strong><div class="chart-track"><i style="width:${Math.round(o.pending/total*100)}%"></i></div></div></div></div><div class="panel mini"><h3>Vehicle Status Overview</h3><div class="inside">Vehicle status analytics will be expanded in later phases.</div></div><div class="panel mini"><h3>STO Overview</h3><div class="inside sto-cards"><div><span>Core Pending</span><strong>${calculateCorePending()??'—'}</strong></div><div><span>Partial Pending</span><strong>${calculatePartialPending()??'—'}</strong></div></div></div><div class="panel mini"><h3>Loading Point Match</h3><div class="inside"><div class="mapping-form"><input id="mappingSource" placeholder="TOLAGAON LOADING"><span>=</span><input id="mappingTarget" placeholder="Tolagaon"><button class="btn primary" id="addLoadingMapping">+ Add Mapping</button></div><div id="loadingMappingList">${getLoadingMappings().map(m=>`<div class="mapping-row"><span>${esc(m.source)}</span><strong>=</strong><span>${esc(m.target)}</span><button class="btn" data-edit-map="${esc(m.id)}">Edit</button><button class="btn" data-delete-map="${esc(m.id)}">Delete</button></div>`).join('')||'<div style="font-size:11px;color:#697781">No Loading Point mappings saved.</div>'}</div></div></div><div class="panel mini"><h3>Alerts / Exceptions</h3><div class="inside">${a.length?`<div class="tableWrap"><table class="table erp-table dashboard-alert-table"><thead><tr><th>Date</th><th>Name</th><th>Loading</th><th>Remarks</th></tr></thead><tbody>${a.map(r=>`<tr><td>${esc(dateDisplay(r.date))}</td><td>${esc(r.name)}</td><td>${esc(r.loading)}</td><td>${esc(r.remarks)}</td></tr>`).join('')}</tbody></table></div>`:'<div class="alert-empty">No alerts to display.</div>'}</div></div></div>`}

  function bulkInput(kind){const id=kind==='planning'?'planningPaste':kind==='status'?'statusPaste':'raipurPaste';const pv=kind==='planning'?state.planningPreview:kind==='status'?state.statusPreview:state.raipurPreview;return `<section class="panel"><div class="panelTitle"><h3>Bulk Paste Data</h3></div><div class="body"><textarea id="${id}" class="pasteBox" placeholder="Paste complete data here...">${esc(kind==='planning'?state.planningPaste:kind==='status'?state.statusPaste:state.raipurPaste)}</textarea><div class="actions"><button class="btn primary" data-preview="${kind}">Preview Bulk Paste</button><button class="btn" data-clear="${kind}">Clear Data</button>${state.message?`<span class="inline-message">${esc(state.message)}</span>`:''}</div>${pv?bulkPreviewHtml(pv,kind):''}</div></section>`}
  function bulkPreviewHtml(pv,kind){const rows=pv.finalRows.slice(0,20);const isStatus=kind==='status';const headers=isStatus?['S No','Demanded Date','Required Date','Location','Loading Pt.','Weight']:['S.No','Date','Loc','Plant','CFA','Weight','STO','Loading'];const existingCount=read(kind==='planning'?KEYS.planning:kind==='status'?KEYS.status:KEYS.raipur,[]).length;return `<div class="bulk-preview"><div class="panelTitle"><strong>Final Preview</strong><span style="float:right">Raw Rows: <b>${pv.rawRowCount}</b> · Final ${isStatus?'Groups':'Plans'}: <b>${pv.finalRowCount}</b> · Existing: <b>${existingCount}</b></span></div><div class="tableWrap"><table class="table"><thead><tr>${headers.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>isStatus?`<tr><td>${r.serialNo}</td><td>${esc(dateDisplay(r.demandedDate))}</td><td>${esc(r.requiredDate)}</td><td>${esc(r.location)}</td><td>${esc(r.loadingPoint)}</td><td>${esc(r.weight)}</td></tr>`:`<tr><td>${r.serialNo}</td><td>${esc(dateDisplay(r.date))}</td><td>${esc(Array.isArray(r.loc)?r.loc[0]||'—':r.loc)}</td><td>${esc(Array.isArray(r.plant)?r.plant[0]||'':r.plant)}</td><td>${esc(r.cfa)}</td><td>${esc(r.weight)}</td><td>${Array.isArray(r.sto)?r.sto.map(v=>`<div>${esc(v)}</div>`).join(''):esc(r.sto)}</td><td>${esc(r.loading)}</td></tr>`).join('')||`<tr><td colspan="${headers.length}" class="empty-cell">No grouped records were produced.</td></tr>`}</tbody></table></div><div class="body preview-actions"><button class="btn" data-import-mode="append" data-import="${kind}" ${pv.finalRowCount?'':'disabled'}>APPEND</button><button class="btn primary" data-import-mode="replace" data-import="${kind}" ${pv.finalRowCount?'':'disabled'}>REPLACE</button></div></div>`}
  function filterHtml(kind){const f=kind==='planning'?[['date','Date'],['loc','Loc'],['plant','Plant'],['cfa','CFA'],['loading','Loading'],['sto','STO'],['vehicleNumber','Vehicle Number']]:[['demandedDate','Demanded Date'],['requiredDate','Required Date'],['location','Location'],['loadingPoint','Loading Pt.'],['remarks','Remarks'],['gateSlipNo','Gate Slip No.']];return `<div class="filter-grid">${f.map(([k,l])=>`<div class="filter-field"><label>${l}</label><input data-filter="${k}" value="${esc(state.filters[k]||'')}" placeholder="${l}"></div>`).join('')}</div>`}
  function planningDisplayRows(plan){if(Array.isArray(plan.locations)&&plan.locations.length){const locations=plan.locations.map(l=>({loc:text(l.loc)||'—',stos:unique((Array.isArray(l.stos)?l.stos:splitMulti(l.stos)).filter(validMulti))}));const childRows=locations.flatMap(l=>l.stos.length?l.stos.map(sto=>({loc:l.loc,sto})):([{loc:l.loc,sto:''}]));return {locations,childRows,count:childRows.length||1,locs:locations.map(l=>l.loc),stos:childRows.map(r=>r.sto).filter(Boolean)}}const raw=read(KEYS.planningRaw,[]);const idxs=Array.isArray(plan.rawRowIndexes)?plan.rawRowIndexes:[];const source=idxs.map(i=>raw[i]).filter(Boolean);const locMap=new Map();source.forEach(r=>{const loc=text(r.loc)||'—';const key=norm(loc);let lg=locMap.get(key);if(!lg){lg={loc,stos:[]};locMap.set(key,lg)}lg.stos.push(...(Array.isArray(r.sto)?r.sto:splitMulti(r.sto)).filter(validMulti))});const locations=Array.from(locMap.values()).map(l=>({loc:l.loc,stos:unique(l.stos)}));const childRows=locations.flatMap(l=>l.stos.length?l.stos.map(sto=>({loc:l.loc,sto})):([{loc:l.loc,sto:''}]));return {locations,childRows,count:childRows.length||1,locs:locations.map(l=>l.loc),stos:childRows.map(r=>r.sto).filter(Boolean)}}
  function planningHtml(){let rows=enrichPlanning(),q=(state.search||'').toLowerCase();if(q)rows=rows.filter(r=>JSON.stringify(r).toLowerCase().includes(q));const pc=loadingPendingCounts();return `<section class="panel"><div class="body"><div class="sheetRow"><label>Search:</label><input class="search" id="planningSearch" value="${esc(state.search)}" placeholder="Search Vehicle Planning..."></div></div></section>${bulkInput('planning')}<section class="panel"><div class="panelTitle"><h3>Vehicle Planning Records</h3><p>${rows.length} final plan(s).</p></div><div class="body table-body"><div class="tableWrap"><table class="table erp-table grouped-plan-table"><thead><tr><th>S.No</th><th>Date</th><th>Loc</th><th>Plant</th><th>CFA</th><th>Weight</th><th>STO</th><th>Loading <span class="loading-status">B-${pc.B}<br>T-${pc.T}</span></th><th>Vehicle In</th><th>Vehicle Number</th><th>Vehicle Out</th><th>Slip Number</th></tr></thead><tbody>${rows.length?rows.map(r=>{const d=planningDisplayRows(r),n=d.count,rendered=[];d.locations.forEach(loc=>{const stos=loc.stos.length?loc.stos:[''];stos.forEach((sto,i)=>rendered.push({loc:loc.loc,sto,locFirst:i===0,locSpan:stos.length}))});return rendered.map((x,i)=>{const cells=[];if(i===0){cells.push(`<td rowspan="${n}" class="plan-parent-cell">${esc(r.serialNo)}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(dateDisplay(r.date))}</td>`)}if(x.locFirst)cells.push(`<td rowspan="${x.locSpan}" class="location-parent-cell">${esc(x.loc)}</td>`);if(i===0){cells.push(`<td rowspan="${n}" class="plan-parent-cell">${esc(Array.isArray(r.plant)?r.plant[0]:r.plant)}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(r.cfa)}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(r.weight)}</td>`)}cells.push(`<td>${esc(x.sto)}</td>`);if(i===0){cells.push(`<td rowspan="${n}" class="plan-parent-cell">${esc(r.loading)}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(displayPending(dateDisplay(r.vehicleIn)))}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(displayPending(r.vehicleNumber))}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(displayPending(dateDisplay(r.vehicleOut)))}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(displayPending(r.slipNumber))}</td>`)}return `<tr class="${r.conflict?'conflict-row':''}">${cells.join('')}</tr>`}).join('')}).join(''):`<tr><td colspan="12" class="empty-cell">No vehicle planning data available.</td></tr>`}</tbody></table></div></div></section>`}

  function statusHtml(){let rows=enrichStatus(),q=(state.search||'').toLowerCase();if(q)rows=rows.filter(r=>JSON.stringify(r).toLowerCase().includes(q));return `<section class="panel"><div class="body"><div class="sheetRow"><label>Search:</label><input class="search" id="statusSearch" value="${esc(state.search)}" placeholder="Search Vehicle Status Records..."></div></div></section>${bulkInput('status')}<section class="panel"><div class="panelTitle"><h3>Vehicle Status Records</h3><p>${rows.length} final demand group(s).</p></div><div class="body table-body"><div class="tableWrap"><table class="table erp-table"><thead><tr>${statusColumns.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody>${rows.length?rows.map(r=>`<tr class="${r.conflict?'conflict-row':''}">${[r.serialNo,dateDisplay(r.demandedDate),dateDisplay(r.requiredDate),r.location,r.loadingPoint,r.weight,displayPending(dateDisplay(r.vehicleArrived)),displayPending(dateDisplay(r.vehicleDispatch)),r.remarks,displayPending(r.gateSlipNo)].map(v=>`<td>${esc(v)}</td>`).join('')}</tr>`).join(''):`<tr><td colspan="10" class="empty-cell">No vehicle status records available.</td></tr>`}</tbody></table></div></div></section>`}

  function raipurHtml(){let rows=read(KEYS.raipur,[]),q=(state.search||'').toLowerCase();if(q)rows=rows.filter(r=>JSON.stringify(r).toLowerCase().includes(q));return `<section class="panel pageHead"><div><h1>Raipur Database</h1></div></section>${bulkInput('raipur')}<section class="panel"><div class="panelTitle"><h3>Raipur Records</h3><p>${rows.length} final plan(s).</p></div><div class="body"><div class="sheetRow"><label>Search:</label><input class="search" id="raipurSearch" value="${esc(state.search)}" placeholder="Search Raipur records"></div><div class="tableWrap"><table class="table erp-table grouped-plan-table"><thead><tr>${planningColumns.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody>${rows.length?rows.map(r=>{const d=planningDisplayRows(r),n=d.count,rendered=[];d.locations.forEach(loc=>{const stos=loc.stos.length?loc.stos:[''];stos.forEach((sto,i)=>rendered.push({loc:loc.loc,sto,locFirst:i===0,locSpan:stos.length}))});return rendered.map((x,i)=>{const cells=[];if(i===0){cells.push(`<td rowspan="${n}" class="plan-parent-cell">${esc(r.serialNo)}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(dateDisplay(r.date))}</td>`)}if(x.locFirst)cells.push(`<td rowspan="${x.locSpan}" class="location-parent-cell">${esc(x.loc)}</td>`);if(i===0){cells.push(`<td rowspan="${n}" class="plan-parent-cell">${esc(Array.isArray(r.plant)?r.plant[0]:r.plant)}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(r.cfa)}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(r.weight)}</td>`)}cells.push(`<td>${esc(x.sto)}</td>`);if(i===0){cells.push(`<td rowspan="${n}" class="plan-parent-cell">${esc(r.loading)}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(dateDisplay(r.vehicleIn))}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(r.vehicleNumber)}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(dateDisplay(r.vehicleOut))}</td>`,`<td rowspan="${n}" class="plan-parent-cell">${esc(r.slipNumber)}</td>`)}return `<tr>${cells.join('')}</tr>`}).join('')}).join(''):`<tr><td colspan="12" class="empty-cell">No Raipur database records available.</td></tr>`}</tbody></table></div></div></section>`}
  function excelDateValue(v,header,date1904){const isDateColumn=/date/i.test(String(header||''));if(!isDateColumn)return v??'';if(v instanceof Date)return dateDisplay(v);if(typeof v==='number'&&Number.isFinite(v))return dateDisplay(excelSerialToBusinessDate(v,date1904));if(typeof v==='string')return dateDisplay(v);return''}
  function excelDisplayRow(row,headers,date1904){return headers.map((h,i)=>excelDateValue(row[i],h,date1904))}
  function uploadHtml(){const gi=read(KEYS.gateIn,[]),go=read(KEYS.gateOut,[]),gm=read(KEYS.gateInMeta,{}),om=read(KEYS.gateOutMeta,{});let preview='';if(state.workbook){const sheet=state.workbook.Sheets[state.selectedSheet]||{};const matrix=window.XLSX.utils.sheet_to_json(sheet,{header:1,defval:'',raw:true});const headers=(matrix[0]||[]).map((h,i)=>String(h||`Column ${i+1}`));const data=matrix.slice(1).filter(r=>r.some(v=>text(v))).map(r=>excelDisplayRow(r,headers,state.workbookDate1904)).filter(r=>!state.search||r.some(v=>text(v).toLowerCase().includes(state.search.toLowerCase())));const size=state.pageSize==='all'?Math.max(data.length,1):Number(state.pageSize);const pages=Math.max(1,Math.ceil(data.length/size));state.pageNo=Math.min(state.pageNo,pages);const start=(state.pageNo-1)*size;const vis=data.slice(start,start+size);preview=`<section class="panel"><div class="panelTitle"><h3>Excel Preview</h3><p>Rows: ${data.length} · Columns: ${headers.length}</p></div><div class="body"><div class="sheetRow"><label>Select Sheet:</label><select id="sheetSelect">${state.workbook.SheetNames.map(s=>`<option ${s===state.selectedSheet?'selected':''}>${esc(s)}</option>`).join('')}</select><input id="previewSearch" class="search" value="${esc(state.search)}" placeholder="Search preview"><select id="pageSize"><option value="5">5</option><option value="10">10</option><option value="15" selected>15</option><option value="all">All</option></select><button class="btn primary" id="importGate">Import ${state.uploadRole==='gateIn'?'Gate In':'Gate Out'}</button></div><div class="tableWrap"><table class="table"><thead><tr>${headers.map(h=>`<th>${esc(h)}</th>`).join('')}</tr></thead><tbody>${vis.length?vis.map(r=>`<tr>${headers.map((_,i)=>`<td>${esc(r[i])}</td>`).join('')}</tr>`).join(''):`<tr><td colspan="${headers.length||1}" class="empty-cell">This sheet contains no data.</td></tr>`}</tbody></table></div><div class="pager">Showing ${data.length?start+1:0}–${Math.min(start+size,data.length)} of ${data.length} rows</div></div></section>`}return `<section class="panel pageHead"><div><h1>Excel Upload</h1><p></p></div></section><section class="panel"><div class="body"><div class="drop"><strong>Excel Upload</strong><div>Drag &amp; Drop Excel File Here</div><div style="margin:8px 0;color:#7a8790">or</div><label class="btn primary" for="fileInput">Select Excel File</label><input class="fileinput" id="fileInput" type="file" accept=".xlsx,.xls"><div style="margin-top:8px;font-size:11px;color:#77848c">Supported: .xlsx, .xls</div></div>${state.message?`<div style="margin-top:10px;padding:9px;border:1px solid #d2d9dd;background:#fafbfb;font-size:12px">${esc(state.message)}</div>`:''}<div class="meta"><div><span>Gate In Imported</span><strong>${esc(gm.fileName||'—')}</strong></div><div><span>Gate Out Imported</span><strong>${esc(om.fileName||'—')}</strong></div><div><span>Gate In Rows</span><strong>${gi.length}</strong></div><div><span>Gate Out Rows</span><strong>${go.length}</strong></div><div><span>Current Role</span><strong><select id="uploadRole"><option value="gateIn" ${state.uploadRole==='gateIn'?'selected':''}>Gate In Data</option><option value="gateOut" ${state.uploadRole==='gateOut'?'selected':''}>Gate Out Data</option></select></strong></div></div></div></section>${state.workbook?preview:''}`}
  function restoreFocus(id,start,end){const el=document.getElementById(id);if(el){el.focus();if(typeof start==='number'&&el.setSelectionRange)el.setSelectionRange(start,end)}}   function bind(){document.getElementById('menuBtn')?.addEventListener('click',()=>{state.sidebar=!state.sidebar;render()});document.querySelectorAll('[data-nav]').forEach(b=>b.addEventListener('click',()=>{state.page=b.dataset.nav;state.search='';state.filters={};render()}));document.getElementById('saveHeader')?.addEventListener('click',saveHeaderSettings);const fp=document.getElementById('planningSearch');if(fp)fp.addEventListener('input',e=>{state.search=e.target.value;render();restoreFocus('planningSearch')});const fs=document.getElementById('statusSearch');if(fs)fs.addEventListener('input',e=>{state.search=e.target.value;render();restoreFocus('statusSearch')});const rp=document.getElementById('raipurSearch');if(rp)rp.addEventListener('input',e=>{state.search=e.target.value;render();restoreFocus('raipurSearch')});const file=document.getElementById('fileInput');if(file)file.addEventListener('change',e=>handleFile(e.target.files[0]));const role=document.getElementById('uploadRole');if(role)role.addEventListener('change',()=>{state.uploadRole=role.value;render()});const sel=document.getElementById('sheetSelect');if(sel)sel.addEventListener('change',()=>{state.selectedSheet=sel.value;state.search='';state.pageNo=1;render()});const s=document.getElementById('previewSearch');if(s)s.addEventListener('input',e=>{state.search=e.target.value;state.pageNo=1;render();restoreFocus('previewSearch')});const ps=document.getElementById('pageSize');if(ps)ps.addEventListener('change',()=>{state.pageSize=ps.value==='all'?'all':Number(ps.value);state.pageNo=1;render()});document.querySelectorAll('[data-preview]').forEach(b=>b.addEventListener('click',()=>buildPreview(b.dataset.preview)));document.querySelectorAll('[data-clear]').forEach(b=>b.addEventListener('click',()=>clearPaste(b.dataset.clear)));document.querySelectorAll('[data-import]').forEach(b=>b.addEventListener('click',()=>importPreview(b.dataset.import,b.dataset.importMode||'replace')));document.getElementById('importGate')?.addEventListener('click',importCurrentSheet);const top=document.getElementById('backToTop');if(top)top.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));const syncTop=()=>{if(top)top.classList.toggle('hidden',window.scrollY<260)};syncTop();window.addEventListener('scroll',syncTop,{passive:true});const ms=document.getElementById('mappingSource'),mt=document.getElementById('mappingTarget'),am=document.getElementById('addLoadingMapping');if(am)am.addEventListener('click',()=>{const r=saveLoadingMapping(ms?.value,mt?.value);if(!r.ok){state.message=r.message;render();return}state.message='Loading Point mapping saved.';render()});document.querySelectorAll('[data-delete-map]').forEach(b=>b.addEventListener('click',()=>{if(window.confirm('Delete this Loading Point mapping?'))deleteLoadingMapping(b.dataset.deleteMap);render()}));document.querySelectorAll('[data-edit-map]').forEach(b=>b.addEventListener('click',()=>{const m=getLoadingMappings().find(x=>x.id===b.dataset.editMap);if(!m)return;const source=window.prompt('Source Loading Point',m.source);if(source===null)return;const target=window.prompt('Match With',m.target);if(target===null)return;const r=updateLoadingMapping(m.id,source,target);if(!r.ok)state.message=r.message;render()}));}
  function buildPreview(kind){const id=kind==='planning'?'planningPaste':kind==='status'?'statusPaste':'raipurPaste';const raw=document.getElementById(id)?.value||'';state[kind+'Paste']=raw;const p=preview(kind,raw);if(!p.rawRowCount){state.message='No valid rows found in the pasted data.';state[kind+'Preview']=null}else{state[kind+'Preview']=p;state.message='Preview ready. Review the grouped result before importing.'}render()}
  function clearPaste(kind){const key=kind==='planning'?KEYS.planning:kind==='status'?KEYS.status:KEYS.raipur;localStorage.removeItem(key);localStorage.removeItem(kind==='planning'?KEYS.planningRaw:kind==='status'?KEYS.statusRaw:KEYS.raipurRaw);state[kind+'Paste']='';state[kind+'Preview']=null;state.message=`${kind==='planning'?'Vehicle planning':kind==='status'?'Vehicle status':'Raipur database'} data cleared.`;render()}
  function importPreview(kind,mode){const p=state[kind+'Preview'];if(!p?.finalRows?.length)return;const key=kind==='planning'?KEYS.planning:kind==='status'?KEYS.status:KEYS.raipur;const rawKey=kind==='planning'?KEYS.planningRaw:kind==='status'?KEYS.statusRaw:KEYS.raipurRaw;const existingFinal=read(key,[]),existingRaw=read(rawKey,[]);if(mode==='replace'){const ok=window.confirm(`Replace existing data?\n\nYou currently have ${existingFinal.length} ${kind==='status'?'groups':'plans'}. Replacing will remove the existing dataset.`);if(!ok)return}const combinedRaw=mode==='append'?existingRaw.concat(p.rows.map((row,i)=>i===0?{...row,__batchBoundary:true}:row)):p.rows;const final=kind==='planning'?aggregatePlanning(combinedRaw):kind==='raipur'?aggregateRaipur(combinedRaw):aggregateStatus(combinedRaw);write(key,final);write(rawKey,combinedRaw.map(row=>{const n={...row};delete n.__batchBoundary;return n}));state[kind+'Paste']='';state[kind+'Preview']=null;state.message=`${mode==='append'?'Appended':'Replaced'} data. Final ${kind==='status'?'groups':'plans'}: ${final.length}.`;render()}
  function handleFile(file){if(!file)return;state.file=file;if(!/\.(xlsx|xls)$/i.test(file.name)){state.message='Invalid file format.
Please upload an .xlsx or .xls file.';render();return}loadXlsx().then(X=>{const reader=new FileReader();reader.onload=e=>{try{state.workbook=X.read(new Uint8Array(e.target.result),{type:'array',cellDates:false,raw:true,cellNF:true});state.workbookDate1904=Boolean(state.workbook?.Workbook?.WBProps?.date1904);state.selectedSheet=state.workbook.SheetNames[0]||'';state.pageNo=1;state.message='Workbook ready for preview.';render()}catch(err){console.error(err);state.message='Unable to read the Excel workbook.';render()}};reader.readAsArrayBuffer(file)}).catch(()=>{state.message='Excel parser could not be loaded.';render()})}
  function importCurrentSheet(){if(!state.workbook)return;const ws=state.workbook.Sheets[state.selectedSheet],matrix=window.XLSX.utils.sheet_to_json(ws,{header:1,defval:'',raw:true}),headers=(matrix[0]||[]).map((h,i)=>String(h||`Column ${i+1}`)),rows=matrix.slice(1).filter(r=>r.some(v=>text(v))).map(r=>{const values=excelDisplayRow(r,headers,state.workbookDate1904);return Object.fromEntries(headers.map((h,i)=>[h,values[i]??'']))}),role=state.uploadRole,slip=header(headers,gateAliases.slip);if(!slip){state.message='Gate Slip Number column could not be detected.';render();return}const required=role==='gateIn'?[gateAliases.sto,gateAliases.inDate,gateAliases.vehicle]:[gateAliases.outDate];if(required.some(a=>!header(headers,a))){state.message=`Missing required ${role==='gateIn'?'Gate In':'Gate Out'} header(s).`;render();return}const meta={fileName:state.file?.name||'',sheetName:state.selectedSheet,importedAt:new Date().toISOString()};write(role==='gateIn'?KEYS.gateIn:KEYS.gateOut,rows);write(role==='gateIn'?KEYS.gateInMeta:KEYS.gateOutMeta,meta);state.message=`${role==='gateIn'?'Gate In':'Gate Out'} data imported successfully.`;render()}
  function loadXlsx(){if(window.XLSX)return Promise.resolve(window.XLSX);return new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js';s.onload=()=>resolve(window.XLSX);s.onerror=reject;document.head.appendChild(s)})}
  function esc(v){return String(v==null?'':v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;')}
  ensureDateDataVersion(); if(!localStorage.getItem(KEYS.headerSettings)) write(KEYS.headerSettings,state.header);
  window.addEventListener('vlc-data-changed',render);render();
})();
